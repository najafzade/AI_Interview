
{
  "entities": {
    "InterviewSession": {
      "title": "Interview Session",
      "description": "Represents a single interview instance for a candidate, including transcript and state.",
      "type": "object",
      "properties": {
        "candidateName": { "type": "string" },
        "skill": { "type": "string" },
        "status": { "type": "string", "enum": ["IN_PROGRESS", "COMPLETED"] },
        "state": { "type": "object" },
        "promptVersion": { "type": "string" },
        "audioDataUri": { "type": "string", "description": "The latest audio recording from the candidate." },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["candidateName", "skill", "status", "state", "promptVersion", "createdAt"]
    },
    "Feedback": {
      "title": "Evaluator Feedback",
      "description": "Persisted feedback and ratings from evaluators.",
      "type": "object",
      "properties": {
        "interviewId": { "type": "string" },
        "quality": { "type": "number" },
        "fairness": { "type": "number" },
        "relevance": { "type": "number" },
        "issues": { "type": "array", "items": { "type": "string" } },
        "comments": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["interviewId", "quality", "fairness", "relevance", "issues", "comments", "createdAt"]
    },
    "PromptConfig": {
      "title": "Prompt Configuration",
      "description": "Versioned system instructions for the interview agent.",
      "type": "object",
      "properties": {
        "version": { "type": "string" },
        "isActive": { "type": "boolean" },
        "instructions": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["version", "isActive", "instructions", "createdAt"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/sessions/{sessionId}": {
      "schema": { "$ref": "#/backend/entities/InterviewSession" },
      "description": "Collection of all interview sessions"
    },
    "/feedback/{feedbackId}": {
      "schema": { "$ref": "#/backend/entities/Feedback" },
      "description": "Collection of evaluator feedback"
    },
    "/prompt_configs/{configId}": {
      "schema": { "$ref": "#/backend/entities/PromptConfig" },
      "description": "Versioned system prompts"
    }
  }
}
